
export async function unauthEmail(id: string, timesteamp: string) {
  // Needs implementation. Checkout https://nodemailer.com
}

export async function unauthPush(id: string, timesteamp: string) {
  // Needs implementation
}
